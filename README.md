# messagescan

1. Run crab-parachain subql

    https://github.com/helix-bridge/indexer/tree/master/subql/crab-parachain

    ```bash
    npm run start
    ```

2. Run workers

    ```bash
    npm run run_workers
    ```

3. Run queue monitor

    ```bash
    npm run arena
    ```

4. Send jobs

    ```bash
    node src/index.js
    ```
